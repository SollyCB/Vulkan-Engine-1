#pragma once

struct StringBuffer {};

// Static functions are useful for methods which are not specific to the data
// stored in the specific instance of an object.
// For instance, if I want to check if two strings are equal but I want to do
// this even without an instance of the object

struct StringView {};
